{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Run tsor",
            "type": "shell",
            "command": "build/tsor --help"
        },
        {
            "label": "Rebuild tsor",
            "type": "shell",
            "command": "cd build && cmake .. && make -j$(nproc) && ./tsor ~/test.ts --gui"
        }
    ]
}
